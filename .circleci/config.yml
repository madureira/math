version: 2.0
jobs:
  build:
    docker:
      - image: gcc:latest
    steps:
      - checkout
      - run: git submodule init
      - run: git submodule update
      - run: make test

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
